# ============================================================
# .env - Environment Variables for the Backend
# ============================================================
# This file stores sensitive configuration.
# Update the values below to match YOUR MySQL setup.
# ============================================================

# --- MySQL Database Connection ---
# DB_HOST    : Where MySQL is running (localhost for your machine)
# DB_USER    : Your MySQL username (usually "root" on local setup)
# DB_PASSWORD: Your MySQL password (set during MySQL installation)
# DB_NAME    : Name of the database we created in schema.sql
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=                            # put your sql password here
DB_NAME=hotel_management

# --- Server Port ---
# The backend will run on this port.
# Both frontends are configured to talk to port 8000.
PORT=8000
